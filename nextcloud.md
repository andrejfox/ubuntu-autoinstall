## Nextcloud install instructions

```bash
# if curl is not installed yet
sudo apt update && sudo apt install -y curl && bash <(curl -fsSL https://raw.githubusercontent.com/andrejfox/ubuntu-autoinstall/main/nex.sh)

# script to install nextcloud and ustostart it
bash <(curl -fsSL https://raw.githubusercontent.com/andrejfox/ubuntu-autoinstall/main/nex.sh)
```
